{
    "name": "@polymedia/profile-web",
    "author": "juzybits <https://twitter.com/juzybits>",
    "private": true,
    "dependencies": {
        "@mysten/sui.js": "0.23.0",
        "ethos-connect": "0.0.69",
        "react": "18",
        "react-dom": "18",
        "react-router-dom": "6.7.0"
    },
    "devDependencies": {
        "@parcel/transformer-less": "2.8.3",
        "@types/react": "18.0.27",
        "@types/react-dom": "18",
        "buffer": "6.0.3",
        "parcel": "2.8.3",
        "process": "0",
        "react-devtools": "latest"
    },
    "scripts": {
        "serve": "rm -rf .parcel-cache/ dist/ && parcel src/index.html",
        "build": "rm -rf .parcel-cache/ dist/ && parcel build --no-source-maps src/index.html && cp src/static/* dist/",
        "deploy-dev": "yarn build && wrangler pages publish dist/ --project-name profile --branch dev",
        "deploy-prod": "yarn build && wrangler pages publish dist/ --project-name profile --branch main"
    }
}
